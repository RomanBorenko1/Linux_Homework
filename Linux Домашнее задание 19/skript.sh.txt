#!/bin/bash

mkdir -p /opt/folder_1 /opt/folder_2

for i in {1..100}; do
    FILE="/opt/folder_1/$RANDOM.txt"
    touch "$FILE"
    NAME="${FILE##*/}"
    NUMBER="${NAME%.txt}"
    if [ $((NUMBER % 2)) -eq 0 ]; then
        mv "$FILE" /opt/folder_2/
    fi
done




