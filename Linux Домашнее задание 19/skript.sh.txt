#!/bin/bash

SOURCE_DIR="$HOME/290724-ptm/Roman_Borenko/folder_1"
DEST_DIR="$HOME/290724-ptm/Roman_Borenko/folder_2"

mkdir -p "$SOURCE_DIR" "$DEST_DIR"

for i in {1..100}; do
    FILE="$SOURCE_DIR/$RANDOM.txt"
    touch "$FILE"
    NUMBER="${FILE##*/}"
    NUMBER="${NUMBER%.txt}"
    if [ $((NUMBER % 2)) -eq 0 ]; then
        mv "$FILE" "$DEST_DIR/"
    fi
done
